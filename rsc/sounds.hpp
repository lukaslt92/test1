//--- Used for fail over if OFPS Core not loaded
class CfgSounds {
    sounds[] = {nomodsound};
    class nomodsound {
        name = "nomodsound";
        sound[] = {"", 0, 0};
        titles[] = {};
    };
};